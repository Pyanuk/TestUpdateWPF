<Window x:Class="TestAutoUpdate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestAutoUpdate"
        mc:Ignorable="d"
        Title="Test Auto Update" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <StackPanel>
                <TextBlock Text="Test Auto Update Application" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"/>
                <TextBlock x:Name="VersionText" 
                          Text="Version 1.0.0" 
                          FontSize="14" 
                          Foreground="White" 
                          Opacity="0.9"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center">
            <TextBlock Text="Приложение с автоматическим обновлением" 
                      FontSize="18" 
                      Margin="0,0,0,20"
                      HorizontalAlignment="Center"/>
            
            <Button x:Name="CheckUpdateButton" 
                   Content="Проверить обновления" 
                   Width="200" 
                   Height="40" 
                   FontSize="14"
                   Click="CheckUpdateButton_Click"
                   Background="#4CAF50"
                   Foreground="White"
                   BorderThickness="0"
                   Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                           CornerRadius="5"
                                           Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <TextBlock x:Name="StatusText" 
                      Text="" 
                      FontSize="12" 
                      Margin="0,10,0,0"
                      HorizontalAlignment="Center"
                      Foreground="Gray"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#f5f5f5" Padding="10">
            <TextBlock Text="Автообновление через Squirrel.Windows" 
                      FontSize="11" 
                      Foreground="Gray"
                      HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
